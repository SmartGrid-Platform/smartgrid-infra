apiVersion: v1
kind: Service
metadata:
  name: alert-service
  namespace: production
  labels:
    app: alert-service
    environment: production
spec:
  selector:
    app: alert-service
  ports:
    - name: http
      port: 3005
      targetPort: 3005
      protocol: TCP
  type: ClusterIP
