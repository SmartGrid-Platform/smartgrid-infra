apiVersion: v1
kind: Service
metadata:
  name: consumer-service
  namespace: production
  labels:
    app: consumer-service
    environment: production
spec:
  selector:
    app: consumer-service
  ports:
    - name: http
      port: 3002
      targetPort: 3002
      protocol: TCP
  type: ClusterIP
