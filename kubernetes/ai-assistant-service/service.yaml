apiVersion: v1
kind: Service
metadata:
  name: ai-assistant-service
  namespace: production
  labels:
    app: ai-assistant-service
    environment: production
spec:
  selector:
    app: ai-assistant-service
  ports:
    - name: http
      port: 4004
      targetPort: 4004
      protocol: TCP
  type: ClusterIP
